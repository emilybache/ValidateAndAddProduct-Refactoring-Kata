#include "ProductFormData.h"

ProductFormData::ProductFormData(std::string name, std::string type, float weight, float suggestedPrice,
                                 bool packagingRecyclable) : name(name), type(type), weight(weight),
                                 suggestedPrice(suggestedPrice), packagingRecyclable(packagingRecyclable) {

}
